# Core (all have Python 3.14 wheels)
fastapi>=0.115
uvicorn>=0.30
python-multipart>=0.0.9
markdown-it-py>=3.0
mdit-py-plugins>=0.4
pygments>=2.17
numpy>=1.26

# Optional, best-effort. The app runs fully without these — semantic search
# automatically upgrades to a transformer model if either of these installs.
# Install with:  pip install -r requirements-optional.txt
